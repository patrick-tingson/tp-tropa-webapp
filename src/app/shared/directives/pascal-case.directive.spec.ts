import { PascalCaseDirective } from './pascal-case.directive';

describe('PascalCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new PascalCaseDirective();
    expect(directive).toBeTruthy();
  });
});
